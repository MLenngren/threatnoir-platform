// Centralized defaults for public site identity.
// Keep literals here so we don't duplicate brand strings across the codebase.

export const DEFAULT_SITE_NAME = 'ThreatNoir'
export const DEFAULT_SITE_TAGLINE = 'Curated security intelligence'

// IMPORTANT: Default must never point to the canonical operator domain.
export const DEFAULT_SITE_URL = 'https://example.com'
